#include "main.h"
/**
 * _abs - computes the absolute value of an integer
 *
 * Return: integer
 */
int _abs(int z)
{
	if(z > 0)
	{
		return (z);
	}
	else if(z == 0)
	{
		return (z);
	}
	else
	{
		return (-z);
	}
}
